-- Formatting is handled by the LazyVim extras:
--   - lazyvim.plugins.extras.formatting.prettier (runs when .prettierrc etc. exists)
--   - lazyvim.plugins.extras.formatting.biome    (runs when biome.json exists)
-- See lua/config/options.lua for vim.g.lazyvim_prettier_needs_config = true
return {}
